# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-single
*.local
.env

# BGG raw scrape output (per-thread raw bodies + metadata, ~3.6 MB) — only
# the curated derivative `forum-knowledge.json` is committed for deployment.
# Refresh locally via `npm run refresh:bgg`.
src/data/blue-water-navy/_fragments/bgg-bwn-raw.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
docs/BWN/BWN-Action-and-PlayAid-Draft.html
